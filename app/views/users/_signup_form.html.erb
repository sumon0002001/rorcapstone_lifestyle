<div class="card">
  <div class="card-header text-center h2">
    LifeStyle
  </div>
  <div class="card-body">
    <%= form_for @user do |f| %>
      <%= render "shared/errors", object: @user %>
      <div class="field form-group">
        <%= f.label :username %><br />
        <%= f.text_field :username, required: true, minlength: 4, class: "form-control" %>
      </div>
      <div class="field form-group">
        <%= f.label :password %>
        <%= f.password_field :password, required: true, minlength: 6, class: "form-control" %>
      </div>
      <div class="actions">
        <%= f.submit "Sign up", class: 'form-control btn btn-success btn-block' %>
      </div>
    <% end %>
  </div>
</div>
